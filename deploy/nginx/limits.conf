# Put this file into /etc/nginx/conf.d/ (http context), not inside a server {} block.
# Rate-limit zones are declared once and then referenced from server/location configs.

limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=login:10m rate=2r/s;
